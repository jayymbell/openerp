<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Actions
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to 'New Customer', new_customer_organization_path(organization_id: @organization.id), class: "dropdown-item", remote: true%>
      </div>
      </li>
    </ul>
</nav>
<table class="table">
        <thead>
            <th>ID</th>
            <th>Name</th>
            <th>Is active</th>
            <th></th>
        </thead>
        <tbody>
    <%@organization.customer_organizations.each do |e| %>
            <tr>
                <td><%= link_to e.customer.id, e.customer.person%></td>
                <td><%=e.customer.person.name%></td>
                <td><%=boolean_icon(e.is_active)%></td>
                <td><%=delete_btn(e)%></td>
            </tr>
    <%end%>
    </tbody>
</table>