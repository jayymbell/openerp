<%= form_with(model: purchase_order_product_assembly, local: true) do |form| %>
  <% if purchase_order_product_assembly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase_order_product_assembly.errors.count, "error") %> prohibited this purchase_order_product_assembly from being saved:</h2>

      <ul>
        <% purchase_order_product_assembly.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :purchase_order_id %>
    <%= form.text_field :purchase_order_id %>
  </div>

  <div class="field">
    <%= form.label :product_assembly_id %>
    <%= form.text_field :product_assembly_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
