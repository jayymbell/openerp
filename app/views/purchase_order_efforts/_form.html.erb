<%= form_with(model: purchase_order_effort, local: true) do |form| %>
  <% if purchase_order_effort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase_order_effort.errors.count, "error") %> prohibited this purchase_order_effort from being saved:</h2>

      <ul>
        <% purchase_order_effort.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :purchase_order_service_id %>
    <%= form.text_field :purchase_order_service_id %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
