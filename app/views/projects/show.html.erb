<h1>Projects</h1>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Actions
        </a>
        <div class="dropdown-menu dropdown-menu-right dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to 'Edit', edit_project_path(@project), class: 'dropdown-item', remote: true %>
          <%if @project.is_active%>
            <%= form_for @project, remote: true do |form| %>
              <%= form.hidden_field :is_active, value: false %>
              <%= form.submit 'Deactivate', class: 'dropdown-item text-danger', data: { confirm: 'Are you sure?' } %>
            <%end%>
          <%else%>
            <%= form_for @project, remote: true do |form| %>
              <%= form.hidden_field :is_active, value: true %>
              <%= form.submit 'Activate', class: 'dropdown-item text-success', data: { confirm: 'Are you sure?' } %>
            <%end%>
          <%end%>
        </div>
      </li>
    </ul>
</nav>
<%= render 'profile'%>
<ul class="nav nav-tabs" id="myTab" role="tablist" style="padding-top: 10px;">
    <li class="nav-item">
      <a class="nav-link active" id="people-tab" data-toggle="tab" href="#people" role="tab" aria-controls="people" aria-selected="true">People</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="purchase_orders-tab" data-toggle="tab" href="#purchase_orders" role="tab" aria-controls="purchase_orders" aria-selected="true">Purchase Orders</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="invoices-tab" data-toggle="tab" href="#invoices" role="tab" aria-controls="invoices" aria-selected="true">Invoices</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="work-orders-tab" data-toggle="tab" href="#work-orders" role="tab" aria-controls="work-orders" aria-selected="true">Work Orders</a>
    </li>
</ul>
<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="people" role="tabpanel" aria-labelledby="people-tab">
      <div class="container-fluid" style="padding-top: 10px;">
        <%= render 'people'%>
      </div>
    </div>
    <div class="tab-pane fade" id="purchase_orders" role="tabpanel" aria-labelledby="purchase_orders-tab">
      <div class="container-fluid" style="padding-top: 10px;">
        <%= render 'purchase_orders'%>
      </div>
    </div>
    <div class="tab-pane fade" id="invoices" role="tabpanel" aria-labelledby="invoices-tab">
      <div class="container-fluid" style="padding-top: 10px;">
        <%= render 'invoices'%>
      </div>
    </div>
        <div class="tab-pane fade" id="work-orders" role="tabpanel" aria-labelledby="work-orders-tab">
      <div class="container-fluid" style="padding-top: 10px;">
        <%= render 'work_orders'%>
      </div>
    </div>
</div>

