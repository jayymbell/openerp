<%= form_with(model: service_employee, local: true) do |form| %>
  <% if service_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_employee.errors.count, "error") %> prohibited this service_employee from being saved:</h2>

      <ul>
        <% service_employee.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id %>
  </div>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.text_field :service_id %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.check_box :is_active %>
  </div>

  <div class="field">
    <%= form.label :is_primary %>
    <%= form.check_box :is_primary %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
