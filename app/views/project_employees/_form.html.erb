<%= form_with(model: project_employee, local: true) do |form| %>
  <% if project_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_employee.errors.count, "error") %> prohibited this project_employee from being saved:</h2>

      <ul>
        <% project_employee.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.check_box :is_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
