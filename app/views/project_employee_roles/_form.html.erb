<%= form_with(model: project_employee_role, local: true) do |form| %>
  <% if project_employee_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_employee_role.errors.count, "error") %> prohibited this project_employee_role from being saved:</h2>

      <ul>
        <% project_employee_role.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_employee_id %>
    <%= form.text_field :project_employee_id %>
  </div>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.text_field :role_id %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.check_box :is_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
