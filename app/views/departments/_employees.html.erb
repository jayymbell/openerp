<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Actions
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to 'New Employee', new_employee_department_path(department_id: @department.id), class: "dropdown-item", remote: true%>
      </div>
      </li>
    </ul>
</nav>
<table class="table">
        <thead>
            <th>ID</th>
            <th>Name</th>
            <th>Is active</th>
            <th></th>
            <th></th>
        </thead>
        <tbody>
    <%@department.employee_departments.each do |e| %>
            <tr>
                <td><%= link_to e.employee.id, e.employee.person%></td>
                <td><%=e.employee.person.name%></td>
                <td><%=boolean_icon(e.is_active)%></td>
                <td>
                <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#employee-allocation-modal-<%=e.employee.id%>">
                  Allocations
                </button>
                <%= render 'allocations', employee: e.employee%>
                </td>
                <td><%=delete_btn(e)%></td>
            </tr>
    <%end%>
    </tbody>
</table>